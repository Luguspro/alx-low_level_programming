#include<stio.h>
/**
 * main - program that prints exactly with proper grammar, but the outcome is a piece of art using printf function
 * return: o(success)
 */
int main(void)
{printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
